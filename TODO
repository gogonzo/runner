+ streak_run for logical counting TRUE only - other are zeros
+ streak_run for k=n non-recursive
+ countt_run (counting TRUE)
+ table_run (counting number of elements in window)
+ counting streaks in window
+ add tests for all functions

+ calculate if [idx] windowing (with set of NA's) is faster than current

+ mean_run and sum_run for varying k
+ concat_run
+ implement window functions on POSIXct, or find solution using available functions
+ running devainces from y value (eg mean)
+ running non-na number

+ zamienić funkcje cum_min i cum_max na jedną cum_extreme - a przy wywoływaniu funkcji zmienić znak na +/-
