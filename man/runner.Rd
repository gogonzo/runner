% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{runner}
\alias{runner}
\alias{runner.default}
\alias{runner.data.frame}
\alias{runner.grouped_df}
\alias{runner.matrix}
\alias{runner.xts}
\title{Apply function on running windows}
\usage{
runner(
  x,
  f = function(x) x,
  k = integer(0),
  lag = integer(1),
  idx = integer(0),
  at = integer(0),
  na_pad = FALSE,
  simplify = TRUE,
  cl = NULL,
  ...
)

\method{runner}{default}(
  x,
  f = function(x) x,
  k = integer(0),
  lag = integer(1),
  idx = integer(0),
  at = integer(0),
  na_pad = FALSE,
  simplify = TRUE,
  cl = NULL,
  ...
)

\method{runner}{data.frame}(
  x,
  f = function(x) x,
  k = attr(x, "k"),
  lag = if (!is.null(attr(x, "lag"))) attr(x, "lag") else integer(1),
  idx = attr(x, "idx"),
  at = attr(x, "at"),
  na_pad = if (!is.null(attr(x, "na_pad"))) attr(x, "na_pad") else FALSE,
  simplify = TRUE,
  cl = NULL,
  ...
)

\method{runner}{grouped_df}(
  x,
  f = function(x) x,
  k = attr(x, "k"),
  lag = if (!is.null(attr(x, "lag"))) attr(x, "lag") else integer(1),
  idx = attr(x, "idx"),
  at = attr(x, "at"),
  na_pad = if (!is.null(attr(x, "na_pad"))) attr(x, "na_pad") else FALSE,
  simplify = TRUE,
  cl = NULL,
  ...
)

\method{runner}{matrix}(
  x,
  f = function(x) x,
  k = integer(0),
  lag = integer(1),
  idx = integer(0),
  at = integer(0),
  na_pad = FALSE,
  simplify = TRUE,
  cl = NULL,
  ...
)

\method{runner}{xts}(
  x,
  f = function(x) x,
  k = integer(0),
  lag = integer(1),
  idx = integer(0),
  at = integer(0),
  na_pad = FALSE,
  simplify = TRUE,
  cl = NULL,
  ...
)
}
\arguments{
\item{x}{(\code{vector}, \code{data.frame}, \code{matrix}, \code{xts}, \code{grouped_df})\cr
input data.}

\item{f}{(\code{function})\cr
Function applied to each window. Defaults to identity (\code{function(x) x}).}

\item{k}{(\code{integer} or \code{character})\cr
Window size. Single value or vector of \code{length(x)}. Omit for cumulative
windows. Accepts time-interval strings (e.g. \code{"5 days"}) when \code{idx} is set.}

\item{lag}{(\code{integer} or \code{character})\cr
Window shift. Positive shifts back, negative shifts forward. Single value
or vector of \code{length(x)}. Accepts time-interval strings when \code{idx} is set.}

\item{idx}{(\code{integer}, \code{Date}, \code{POSIXt})\cr
Sorted index of observations. When set, \code{k} and \code{lag} refer to index
distance rather than element count. Must be same length as \code{x}.}

\item{at}{(\code{integer}, \code{Date}, \code{POSIXt}, \code{character})\cr
Indices at which to evaluate windows. Output length equals \code{length(at)}
instead of \code{length(x)}. A single time-interval string (e.g. \code{"month"})
generates a regular sequence over the range of \code{idx}.}

\item{na_pad}{(\code{logical})\cr
If \code{TRUE}, return \code{NA} for windows that extend beyond the data range.}

\item{simplify}{(\code{logical} or \code{character})\cr
Simplify result like \code{\link[base:lapply]{base::sapply()}}. \code{TRUE} returns vector/matrix,
\code{"array"} may return a higher-dimensional array.}

\item{cl}{(\code{cluster}) \emph{experimental}\cr
Parallel cluster from \code{\link[parallel:makeCluster]{parallel::makeCluster()}}.}

\item{...}{additional arguments passed to \code{f}.}
}
\value{
vector with aggregated values for each window. Length equals
\code{length(x)} or \code{length(at)} if specified. Type depends on \code{f}.
}
\description{
Applies any R function \code{f} on running (Column/sliding) windows defined by
\code{k}, \code{lag}, \code{idx} and \code{at}.
}
\details{
\subsection{Window types}{
\subsection{Sliding window (\code{k})}{

\code{k} sets the number of elements in each window. When \code{k} is a single
constant, the window slides along the data with a fixed size. The first
\code{k-1} windows are shorter since there aren't enough preceding elements.

\if{html}{\figure{constantwindow.png}{options: alt="Constant sliding window"}}

\if{html}{\out{<div class="sourceCode">}}\preformatted{# 4-element sliding sum
runner(1:15, k = 4, f = sum)
}\if{html}{\out{</div>}}

If \code{k} is omitted, windows are cumulative — each window grows from the
first element to the current one, similar to \code{\link[=cumsum]{cumsum()}}.

\if{html}{\figure{cumulativewindows.png}{options: alt="Cumulative windows"}}
\if{latex}{\figure{cumulativewindows.pdf}{options: width=7cm}}

\if{html}{\out{<div class="sourceCode">}}\preformatted{# cumulative sum (k omitted)
runner(1:15, f = sum)
}\if{html}{\out{</div>}}

\code{k} can also be a vector of \code{length(x)} to use a different window size
at each position.
}

\subsection{Lag (\code{lag})}{

\code{lag} shifts the window backward (positive values) or forward (negative
values) relative to the current element. Default is \code{lag = 0}. Like \code{k},
\code{lag} can be a single value or a vector of \code{length(x)}.

\if{html}{\figure{laggedwindowklag.png}{options: alt="Lagged window with k and lag"}}

\if{html}{\out{<div class="sourceCode">}}\preformatted{runner(1:15, k = 4, lag = 2, f = sum)
}\if{html}{\out{</div>}}
}

\subsection{Index-based windows (\code{idx})}{

By default, \code{runner} treats elements as equally spaced (index increments
by 1). Real data often has gaps — missing weekends, holidays, irregular
timestamps. Setting \code{idx} makes \code{k} and \code{lag} refer to index distance
instead of element count, so the number of elements per window varies
with the spacing.

For example, a 5-day window (\code{k = 5}) on unevenly-spaced dates will
contain different numbers of observations at each step:

\if{html}{\figure{runningdatewindows.png}{options: alt="Running date windows"}}
\if{latex}{\figure{runningdatewindows.pdf}{options: width=7cm}}

\code{k} and \code{lag} also accept time-interval strings using the same syntax as
\code{seq.POSIXt(by = ...)}, e.g. \code{"5 days"}, \code{"2 weeks"}, \code{"month"}.

\if{html}{\out{<div class="sourceCode">}}\preformatted{idx <- c(4, 6, 7, 13, 17, 18, 18, 21, 27, 31, 37, 42, 44, 47, 48)
runner(idx, k = 5, lag = 1, idx = idx, f = mean)

# equivalent with Date index
runner(idx, k = "5 days", lag = "day", idx = Sys.Date() + idx, f = mean)
}\if{html}{\out{</div>}}
}

\subsection{Evaluation at specific points (\code{at})}{

By default, \code{runner} returns one result per element of \code{x}. Setting \code{at}
restricts evaluation to specific index positions — the output length equals
\code{length(at)}. This is useful when you only need results at certain dates
or milestones, not at every observation.

\if{html}{\figure{runnerat.png}{options: alt="Runner at specific points"}}
\if{latex}{\figure{runnerat.pdf}{options: width=7cm}}

\if{html}{\out{<div class="sourceCode">}}\preformatted{runner(1:15, k = 5, lag = 1, idx = idx, at = c(18, 27, 48, 31), f = mean)
}\if{html}{\out{</div>}}

\code{at} can also be a single time-interval string, which generates a regular
sequence over the \code{idx} range. For example, \code{at = "4 months"} evaluates
at every 4-month interval from \code{min(idx)} to \code{max(idx)}.
}

}

\subsection{Time-interval syntax}{

\code{k}, \code{lag} and \code{at} accept time-interval strings (requires \code{idx} to be set)
using the same syntax as the \code{by} argument in \code{\link[base:seq.POSIXt]{base::seq.POSIXt()}}:
\code{"sec"}, \code{"min"}, \code{"hour"}, \code{"day"}, \code{"week"}, \code{"month"}, \code{"quarter"},
\code{"year"}, or \code{"<n> <unit>s"} (e.g. \code{"5 days"}, \code{"-2 weeks"}).
Both \code{k} and \code{lag} can also be vectors, allowing different window sizes
and shifts at each position.
}

\subsection{Parallel computing}{

Pass a \code{\link[parallel:makeCluster]{parallel::makeCluster()}} object via \code{cl} to run windows in
parallel. Objects referenced inside \code{f} (other than its arguments) must
be exported with \code{\link[parallel:clusterApply]{parallel::clusterExport()}} beforehand. Parallel
execution adds overhead and is only beneficial for expensive computations.
}
}
\examples{

# runner returns windows as is by default
runner(1:10)

# mean on k = 3 elements windows
runner(1:10, f = mean, k = 3)

# mean on k = 3 elements windows with different specification
runner(1:10, k = 3, f = function(x) mean(x, na.rm = TRUE))

# concatenate two columns
runner(
  data.frame(
    a = letters[1:10],
    b = 1:10
  ),
  f = function(x) paste(paste0(x$a, x$b), collapse = "+")
)

# concatenate two columns with additional argument
runner(
  data.frame(
    a = letters[1:10],
    b = 1:10
  ),
  f = function(x, xxx) {
    paste(paste0(x$a, xxx, x$b), collapse = " + ")
  },
  xxx = "..."
)

# number of unique values in each window (varying window size)
runner(letters[1:10],
  k = c(1, 2, 2, 4, 5, 5, 5, 5, 5, 5),
  f = function(x) length(unique(x))
)

# concatenate only on selected windows index
runner(letters[1:10],
  f = function(x) paste(x, collapse = "-"),
  at = c(1, 5, 8)
)

# 5 days mean
idx <- c(4, 6, 7, 13, 17, 18, 18, 21, 27, 31, 37, 42, 44, 47, 48)
runner::runner(
  x = idx,
  k = "5 days",
  lag = 1,
  idx = Sys.Date() + idx,
  f = function(x) mean(x)
)

# 5 days mean at 4-indices
runner::runner(
  x = 1:15,
  k = 5,
  lag = 1,
  idx = idx,
  at = c(18, 27, 48, 31),
  f = mean
)

# runner with data.frame
df <- data.frame(
  a = 1:13,
  b = 1:13 + rnorm(13, sd = 5),
  idx = seq(as.Date("2022-02-22"), as.Date("2023-02-22"), by = "1 month")
)
runner(
  x = df,
  idx = "idx",
  at = "6 months",
  f = function(x) {
    cor(x$a, x$b)
  }
)

# parallel computing
library(parallel)
data <- data.frame(
  a = runif(100),
  b = runif(100),
  idx = cumsum(sample(rpois(100, 5)))
)
const <- 0
cl <- makeCluster(1)
clusterExport(cl, "const", envir = environment())

runner(
  x = data,
  k = 10,
  f = function(x) {
    cor(x$a, x$b) + const
  },
  idx = "idx",
  cl = cl
)
stopCluster(cl)

# runner with matrix
data <- matrix(data = runif(100, 0, 1), nrow = 20, ncol = 5)
runner(
  x = data,
  f = function(x) {
    tryCatch(
      cor(x),
      error = function(e) NA
    )
  }
)
}
